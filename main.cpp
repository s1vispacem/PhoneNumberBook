#include <iostream>
#include "person.h"
#include <string>

int main(int argc, char* argv[])
{
    std::string input;     // will be using through entire program
    bool flagGloball;
    //TODO: add calculating number of contacts by file
    int contactNum=5;
    person* contact=new person[contactNum];
  
    while(flagGloball)
    {
        
    }


    return 0;
}